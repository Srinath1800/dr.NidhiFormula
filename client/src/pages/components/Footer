import React from 'react'
import { Link } from 'react-router-dom'
import { useCart } from '../App'

const Footer = () => {
  const { isAuthenticated, setIsAuthenticated } = useCart()

  const handleLogout = () => {
    setIsAuthenticated(false)
  }

  return (
    <footer className="footer">
      <div className="footer-container">
        <div className="footer-section about">
          <h3>About Dr. Nidhi's Formula</h3>
          <p>Your trusted online pharmacy for quality healthcare products and wellness solutions. We deliver genuine medicines and care to your doorstep.</p>
        </div>
        <div className="footer-section links">
          <h3>Quick Links</h3>
          <ul>
            <li><Link to="/">Home</Link></li>
            {!isAuthenticated && <li><Link to="/login">Login</Link></li>}
            {isAuthenticated && <li><button onClick={handleLogout} style={{background:'none', border:'none', padding:0, color:'white', cursor:'pointer'}}>Logout</button></li>}
            <li><Link to="/cart">Cart</Link></li>
            <li><Link to="/contact">Contact Us</Link></li>
          </ul>
        </div>
        <div className="footer-section contact">
          <h3>Contact Us</h3>
          <p>Phone: +91 12345 67890</p>
          <p>Email: support@drnidhisformula.com</p>
          <p>Address: Hyderabad, India</p>
        </div>
      </div>
      <div className="footer-bottom">
        <p>© 2025 Dr. Nidhi's Formula. All rights reserved.</p>
      </div>
    </footer>
  )
}

export default Footer
